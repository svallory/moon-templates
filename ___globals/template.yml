# https://moonrepo.dev/docs/config/template
$schema: 'https://moonrepo.dev/schemas/template.json'

title: "[prompt]:  "
description: |
  This template does nothing on its own
  It is meant to be extended by templates that need to ask for a  variable

variables:
  :
    prompt: |
      :
    type: enum
    multiple: false
    values:
      - value: a value
        label: "A label"
    